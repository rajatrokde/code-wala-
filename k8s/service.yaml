apiVersion: v1
kind: Service
metadata:
  name: dev-vibes-service
  namespace: dev-vibes
  labels:
    app: dev-vibes
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
      name: http
  selector:
    app: dev-vibes
